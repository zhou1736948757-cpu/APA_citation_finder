# APA_citation_finder — core dependencies
# 安装：python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# 可选源依赖见 requirements-optional.txt（懒加载，缺失不影响核心流）
# LLM 支撑评分/审计可选：.env 配置 USE_LLM_SUPPORT/LLM_API_KEY/LLM_API_ENDPOINT/LLM_MODEL

requests>=2.31
python-docx>=1.1
python-Levenshtein>=0.25
